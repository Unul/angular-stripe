import {StripeSourceDirective} from "./stripe-source.directive";

describe("StripeSourceDirective", () => {
  it("should create an instance", () => {
    const directive = new StripeSourceDirective();
    expect(directive).toBeTruthy();
  });
});
