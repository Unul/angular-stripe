import { StripeWrapperDirective } from "./stripe-wrapper.directive";

describe("StripeWrapperDirective", () => {
  it("should create an instance", () => {
    const directive = new StripeWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
